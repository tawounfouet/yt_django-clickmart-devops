name: CI/CD Pipeline

on:
  push:
    branches: [main, stg, dev]
  pull_request:
    branches: [main, stg, dev]

jobs:
  test-backend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install ruff coverage

      - name: Lint with ruff
        run: |
          cd backend
          ruff check . --ignore F401,E501,E402 || true

      - name: Run tests
        env:
          SECRET_KEY: ci-test-secret-key-not-for-prod
          DEBUG: 'True'
          EMAIL_HOST_USER: test@test.com
          EMAIL_HOST_PASSWORD: test
          ALLOWED_HOSTS: localhost
          CORS_ALLOWED_ORIGINS: http://localhost:5173
        run: |
          cd backend
          python manage.py test --verbosity=2

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Lint
        run: npm run lint || true
        working-directory: frontend

      - name: Test
        run: npx vitest run --config vite.config.js
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend
        env:
          VITE_SERVER_BASE_URL: /api/v1

  deploy-staging:
    needs: [test-backend, test-frontend]
    if: github.ref == 'refs/heads/stg' && github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy Staging to Linode
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LINODE_HOST }}
          username: ${{ secrets.LINODE_USER }}
          key: ${{ secrets.LINODE_SSH_KEY }}
          script: |
            set -e
            cd /opt/clickmart-stg
            echo "=== Pull latest code (stg) ==="
            git fetch origin stg
            git reset --hard origin/stg
            echo "=== Deploy Staging (port 8080) ==="
            docker compose -p clickmart-stg -f docker-compose.yml -f docker-compose.staging.yml up --build -d
            echo "=== Wait for services ==="
            sleep 15
            echo "=== Health check ==="
            docker compose -p clickmart-stg ps
            curl -sf http://localhost:8080/ || (echo "❌ Staging healthcheck failed" && exit 1)
            echo "✅ Staging deployment successful"

  deploy-production:
    needs: [test-backend, test-frontend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy Production to Linode
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LINODE_HOST }}
          username: ${{ secrets.LINODE_USER }}
          key: ${{ secrets.LINODE_SSH_KEY }}
          script: |
            set -e
            cd /opt/clickmart
            echo "=== Pull latest code (main) ==="
            git fetch origin main
            git reset --hard origin/main
            echo "=== Deploy Production (ports 80/443) ==="
            docker compose -p clickmart -f docker-compose.yml -f docker-compose.prod.yml up --build -d
            echo "=== Wait for services ==="
            sleep 15
            echo "=== Health check ==="
            docker compose -p clickmart ps
            curl -sf http://localhost/api/v1/products/ || (echo "❌ Backend healthcheck failed" && exit 1)
            curl -sf http://localhost/ || (echo "❌ Frontend healthcheck failed" && exit 1)
            echo "✅ Production deployment successful"
