Architecture
============

9 Django apps (users, products, carts, orders, core, images, audio, video, documents).

Settings composed via ``django-split-settings`` (8 files + 3 local overrides).

Dependencies managed via ``uv`` (``pyproject.toml``). CI/CD via GitHub Actions (7 jobs).
